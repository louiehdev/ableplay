version: "2"
sql:
  - engine: "postgresql"
    queries: "../data/queries/"
    schema: "migrations/"
    gen:
      go:
        out: "../data/sqlc/"
        sql_package: "pgx/v5"
        emit_json_tags: true
